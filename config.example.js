// Configuration file for HintBot extension
// Copy this file to config.js and add your actual API key

const config = {
  apiKey: "your_together_ai_api_key_here", // Get your API key from https://api.together.xyz/
};

// Export for use in popup.js
window.config = config;
